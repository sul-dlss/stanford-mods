language: ruby
script: rake rspec
rvm:
  - ruby-head
  - 2.2.3  # spotlight
  - 2.2.0
  - 2.1.5
  - 2.0.0
  - 1.9.3  # argo, FRDA
  # we used to use jruby for merged DOR + MARC records, but no more ...
  - jruby-head
matrix:
  allow_failures:
    - rvm: jruby-head

notifications: false
sudo: false
